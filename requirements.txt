# Compatibility file for legacy tooling.
# Preferred setup lives in pyproject.toml via editable installs:
#   python -m pip install -e '.[dev]'
#   python -m pip install -e '.[dev,analysis]'

fastapi>=0.115.11
numpy>=1.24.0
PyYAML>=6.0.2
torch>=2.2.0
transformers>=4.45.0
uvicorn>=0.34.0
pytest>=8.3.5
ruff>=0.9.1
